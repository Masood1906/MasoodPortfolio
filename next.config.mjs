/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  images: {
    unoptimized: true,
  },
  basePath: '/MasoodPortfolio',
  assetPrefix: '/MasoodPortfolio',
};

export default nextConfig;
